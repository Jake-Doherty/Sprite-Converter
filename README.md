# Sprite-Converter
a simple image converter that takes animated sprites located in the showdown folder of pokeAPIs sprite submodule and converts them into vertically stacked 2 frame images, then packs them in a .assets file for use on the bangle.js 2 hackable smart watchatch
